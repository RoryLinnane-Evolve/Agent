<Project>
    <!-- Applies to all projects under this directory except test projects -->
    <PropertyGroup Condition="'$(IsTestProject)' != 'true'">
        <!-- Unified output: bin/{Configuration}/{ProjectName}/{TargetFramework}/ -->
        <IsTestProject>true</IsTestProject>
    </PropertyGroup>
</Project>